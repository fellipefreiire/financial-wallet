import { styled } from "@/styles";

export const ChartContainer = styled('div', {
  background: '$fa200',
  padding: 16,
  maxHeight: '35vh',

  canvas: {
    height: '100%',
    width: '100%',
  }
})